apiVersion: mcm.ibm.com/v1alpha1
kind: PlacementPolicy
metadata:
  name: nginx
  namespace: mcm-nginx
  labels:
    app: nginx
spec:
  clusterReplicas: 1
  clusterSelector:
    matchLabels:
      region: EU
  resourceHint:
    type: cpu
