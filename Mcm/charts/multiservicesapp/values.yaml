# Default values for multiserviceapp.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: arunimi/add
  tag: v1.0.0
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80